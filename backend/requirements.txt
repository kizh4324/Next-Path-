# Pinned to match docs/architecture/tech-stack.md. Update that table, not just this file.

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- Validation & settings ---
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0

# --- Database ---
sqlalchemy[asyncio]==2.0.36
alembic==1.13.3
asyncpg==0.30.0
greenlet==3.1.1

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
# bcrypt is pinned below 4.1 because passlib 1.7.4 reads bcrypt.__about__.__version__,
# which was removed in bcrypt 4.1. Unpinning breaks password hashing at import time.
bcrypt==4.0.1
python-multipart==0.0.20

# --- LLM (FR-16, FR-17) ---
anthropic==1.0.0

# --- Background scheduling (FR-27, P1) ---
apscheduler==3.10.4

# --- HTTP client (used by the Anthropic SDK and by the test suite) ---
httpx==0.27.2
